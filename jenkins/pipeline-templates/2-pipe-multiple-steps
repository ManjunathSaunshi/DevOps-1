pipeline{
    agent any

    stages{
        stage('Build'){
            steps{
                sh 'echo "Hi 2nd pipeline declarative file"'
                
                sh '''
                    echo " Hi, it's inside 3 hipens sh command - allows to execute multiple shell commnds"
                    ls -lah
                    '''
            }
        }
    }
}
